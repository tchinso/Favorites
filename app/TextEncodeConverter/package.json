{
  "name": "text-encoding-lab",
  "version": "2.0.0",
  "private": true,
  "description": "Client-side text encoding detection and conversion for GitHub Pages",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check encoding-engine.js && node --check app.js && node --test tests/*.test.cjs"
  }
}
